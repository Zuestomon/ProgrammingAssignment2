## Initial stub commit to own git repo 
## mCacheMatrix creates a function that stores the values of an inverted matrix
## The second function takes Fn1 input and determines if the inverted matrix exists in cache. If yes use that value, if not then use solve(<matrix>) to generate in

## Write a short comment describing this function

makeCacheMatrix <- function(x = matrix()) {

}


## Write a short comment describing this function

cacheSolve <- function(x, ...) {
        ## Return a matrix that is the inverse of 'x'
}
